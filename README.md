# ECOG project

in progress

## Directory Structure
`notebooks`: Contains notebooks that can be run via Google CoLab.
`utils`: A common set of python utilites that can be imported in notebooks, so that we don't have to copy the same jupyter blocks over and over.

## How-to use functions in the utils folder
!pip install -q git+https://github.com/smcgovern-proj/neuromatch_ecog_project.git
from neuromatch_ecog_project import [function you want to import]
